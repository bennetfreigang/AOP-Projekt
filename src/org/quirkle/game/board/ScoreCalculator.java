package org.quirkle.game.board;

/** Scores tile placements on a {@link Board}. */
public class ScoreCalculator {
    /** @return points earned for the board's pending tiles. @apiNote stub; always returns {@code 0}. */
    public static int calculatePendingTilesScore(Board board) {
        int points = 0;
        return points;
    }
}
